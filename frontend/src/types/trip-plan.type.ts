export interface TripPlan {
    id: number,
    name: string
    image: string
    createdBy: number
    notes: string
    startDate: string
    endDate: string
    status: string
    daysLeft: number
    destination: number
}